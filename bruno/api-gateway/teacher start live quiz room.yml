meta:
  name: teacher start live quiz room
  type: http
  seq: 0

post:
  url: {{baseUrl}}/v1/api/examruntime-service/teacher/live-quizzes/{{roomId}}/start
  body: none
  auth: bearer

auth:bearer:
  token: {{teacherToken}}
