info:
  name: teacher list question collections
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/v1/api/question-service/teacher/subjects/{{subjectId}}/question-collections?ownership=ALL&status=ACTIVE&page=0&size=20"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
