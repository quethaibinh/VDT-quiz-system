info:
  name: teacher search students
  type: http
  seq: 20

http:
  method: GET
  url: "{{url}}/v1/api/auth-service/teacher/students?keyword=&page=0&size=20&sort=studentCode,asc"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
