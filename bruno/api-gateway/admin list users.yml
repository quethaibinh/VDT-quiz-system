info:
  name: admin list users
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/v1/api/admin/auth-service/users?userType=TEACHER&status=ACTIVE&page=0&size=20&sort=fullName,asc"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
