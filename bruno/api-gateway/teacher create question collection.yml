info:
  name: teacher create question collection
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/v1/api/question-service/teacher/subjects/{{subjectId}}/question-collections"
  body:
    type: json
    data: |-
      {
        "name": "Midterm 2026 Pool",
        "description": "Reusable source pool",
        "visibility": "PRIVATE"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
