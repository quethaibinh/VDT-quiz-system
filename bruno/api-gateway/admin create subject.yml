info:
  name: admin create subject
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/v1/api/admin/question-service/subjects"
  body:
    type: json
    data: |-
      {
        "code": "TRIET_HOC",
        "name": "Triet hoc Mac-Lenin",
        "description": "hihi test"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
