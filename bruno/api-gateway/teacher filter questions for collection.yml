info:
  name: teacher filter questions for collection
  type: http
  seq: 9

http:
  method: GET
  url: "{{url}}/v1/api/question-service/teacher/subjects/{{subjectId}}/questions?difficulty=MEDIUM&collectionId={{collectionId}}&membership=NOT_IN&page=0&size=20"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
