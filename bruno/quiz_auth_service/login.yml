info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8081/v1/api/auth-service/login
  body:
    type: json
    data: |-
      {
        "username": "thanh",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
